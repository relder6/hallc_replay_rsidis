Inside this folder is a python script that will strip the rsidis_runlist.dat by the type of run.  To run it, simply do:

python filter_runs_by_type.py

It will prompt you to enter the type of run.  Type this in, and it will generate a skimmed runlist with that type of file.
